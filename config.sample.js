// Copy this file to `config.js` and fill the values to enable Supabase sync.
// DO NOT commit `config.js` with real keys to public repos.

// Example:
// window.SUPABASE_URL = 'https://xyzcompany.supabase.co';
// window.SUPABASE_ANON_KEY = 'your-anon-key-here';

// When present, the site will attempt to initialize Supabase and optionally
// sync reviews to the `reviews` table. See README for schema instructions.

// window.SUPABASE_URL = '';
// window.SUPABASE_ANON_KEY = '';
